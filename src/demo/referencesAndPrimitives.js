export function referencesAndPrimitivesTester() {

    let user = 'john';
    user = user + ' doe'; // old string gets thrown away and this will produce a new string

    //objects and arrays edit the existing structure

    const colors = ["red", "yellow"];
    colors.push("orange");

}